@if (ViewData["Message"] != null)
{
    if (ViewData["Succeeded"] is true)
    {
        <div class="alert alert-success small" role="alert">
            @ViewData["Message"]
        </div>
    }
    else
    {
        <div class="alert alert-danger small" role="alert">
            @ViewData["Message"]
        </div>
    }
}
